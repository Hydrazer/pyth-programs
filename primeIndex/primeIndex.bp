implicit-assign-input

assign
  maxima-var
  str-start 0. str-end

short-for
  range
    2
    power 5 sep 6
  if is-prime negate short-for-var
    assign concat
      maxima-var
      short-for-var
end-everything

index-into
  maxima-var
  implicit-input